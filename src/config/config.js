
// src/config/config.js
import { TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN } from 'react-native-dotenv';

const config = {
  twilio: {
    accountSid:'',
    authToken: '',
  },
};

export default config;
