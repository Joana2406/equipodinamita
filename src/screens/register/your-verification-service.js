// src/screens/register/your-verification-service.js
export const sendWhatsAppVerification = async (phoneNumber) => {
    // Simulación de una llamada para enviar verificación
    console.log(`Enviando verificación a: ${phoneNumber}`);
    // Aquí va tu lógica real para enviar el mensaje
};
